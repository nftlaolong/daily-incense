{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for daily incense desktop windows",
  "windows": ["main", "offer"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
